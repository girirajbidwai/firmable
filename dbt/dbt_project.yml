name: 'company_data_project'
version: '1.0'
config-version: 2

profile: 'company_data_project'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  company_data_project:
    staging:
      materialized: table
      schema: cleaned
    intermediate:
      materialized: table
      schema: cleaned
    marts:
      materialized: table
      schema: cleaned
